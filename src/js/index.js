import '../css/index.scss';
import EPicker from './selector';

/* global ESELECTOR_VERSION GIT_HASH */
console.log(`${'\n'} %c EPicker v${ESELECTOR_VERSION} %c http://epicker.js.org ${'\n'}`, 'color: #00dcff; background: #3a3b3c; padding:5px 0;', 'background: #00dcff; padding:5px 0;');

export default EPicker;
