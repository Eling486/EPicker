import '../css/index.scss';
import ESelector from './selector';

/* global ESELECTOR_VERSION GIT_HASH */
console.log(`${'\n'} %c ESelector v${ESELECTOR_VERSION} %c http://eselector.js.org ${'\n'}`, 'color: #00dcff; background: #3a3b3c; padding:5px 0;', 'background: #00dcff; padding:5px 0;');

export default ESelector;
